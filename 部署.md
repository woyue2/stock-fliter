# 云端轻量统一入口部署指南（Docker 版）

本文档说明如何将 `stock-fliter` 项目的“云端轻量统一入口”（`/cloud` 页面 + Runner + 报告浏览）部署到一台 Linux 服务器上。

---

## 1. 环境准备

### 1.1 服务器要求

- 操作系统：推荐 Ubuntu 20.04 / 22.04（其他 Linux 也可）
- 内存：至少 1G（视股票池大小而定）
- 网络：能访问外部数据源（如 BaoStock）

### 1.2 安装 Docker & Compose

以 Ubuntu 为例：

```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-plugin

# 将当前用户加入 docker 组（执行后退出重新登录）
sudo usermod -aG docker $USER
```

验证：

```bash
docker version
docker compose version
```

---

## 2. 获取项目代码

### 2.1 通过 Git

```bash
cd /srv
git clone <你的仓库地址> stock-fliter
cd stock-fliter
```

或任意你喜欢的目录，例如 `/opt/stock-fliter`，保持项目结构与本地一致：

```text
/srv/stock-fliter
  ├─ PlanToDeploy/
  ├─ api_server.py
  ├─ docker-compose.yml
  ├─ Dockerfile
  └─ ...
```

### 2.2 通过压缩包（可选）

如果服务器不能直接访问 Git，可在本地打包上传：

```bash
# 在本地
cd /home/aa/Park
tar czf stock-fliter.tar.gz stock-fliter

# 上传到服务器（示例）
scp stock-fliter.tar.gz user@server:/srv

# 在服务器
cd /srv
tar xzf stock-fliter.tar.gz
cd stock-fliter
```

---

## 3. Docker 配置说明

项目根目录已经包含：

- `Dockerfile`：构建 Python 3.11 + 依赖的运行镜像
- `docker-compose.yml`：定义了两个服务

核心部分如下（简化版）：

```yaml
version: "3.9"

services:
  stock-cloud:
    build: .
    container_name: stock-cloud
    ports:
      - "5000:5000"
    volumes:
      # 将当前项目挂载到容器 /app，报告与索引会写回宿主机
      - ./:/app
    environment:
      # 使用云端版 Flask 应用 (PlanToDeploy/api_server.py)
      API_APP_MODULE: "PlanToDeploy.api_server:app"
      # 如需在容器内启用每日自动运行 Runner, 可取消下方注释并按需调整时间:
      # AUTO_RUN_ENABLED: "1"
      # AUTO_RUN_HOUR: "3"
      # AUTO_RUN_MINUTE: "0"
```

> 说明：
> - `volumes: ./:/app` 确保容器里生成的报告与索引文件持久化在宿主机当前目录。
> - `API_APP_MODULE: "PlanToDeploy.api_server:app"` 指定使用云端统一入口的 Flask 应用（包含 `/cloud`、`/reports/*` 等路由）。

另一个服务 `stock-cloud-runner` 用于本地一次性测试 Runner，你可以在生产环境里选择不启用它。

---

## 4. 构建与启动服务

在服务器项目根目录执行：

```bash
cd /srv/stock-fliter   # 根据实际路径调整
docker compose build
docker compose up -d stock-cloud
```
或者
sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
cd ~/stock-fliter

# 构建镜像
docker-compose build

# 启动 API 服务（PlanToDeploy.api_server）
docker-compose up -d stock-cloud
查看容器状态与日志：

```bash
docker ps
docker logs --tail=50 stock-cloud
```

如果一切正常，你会在日志中看到类似：

```text
索引文件: /app/PlanToDeploy/stocks_index/2026-02-03/stocks_index.csv
API服务启动: http://localhost:5000
```

---

## 5. 访问统一入口与功能说明

### 5.1 统一入口路径

在浏览器中访问：

- API 首页（调试用）：  
  `http://<服务器IP>:5000/`

- 云端轻量统一入口：  
  `http://<服务器IP>:5000/cloud`

> 注意：`<服务器IP>` 可以是公网 IP，也可以是内网地址，看你如何访问服务器。

### 5.2 `/cloud` 页面功能

统一入口页面包含：

- **搜索框**：按“代码 / 名称”搜索历史命中记录（基于 `stocks_index`）。
- **🔥 热门股票**：按出现次数排序的 Top N。
- **📈 统计信息**：记录数、覆盖日期、各模块统计等。
- **📋 云端轻量报告列表**：
  - 每一行是一个“数据日期”（如 `2026‑02‑03`）。
  - 列为 `玄学(云端) / TD九底(云端) / 新指标(云端)`。
  - 有报告时显示“查看”，链接形如：
    ```text
    /reports/output/cloud_td/2026-02-03/19-56-17/summary_20260203_195617.html
    ```
  - 无信号时显示“无信号”（灰色，不跳转）。

- **云端 Runner 控制面板**：
  - 参数：
    - `test` 模式（小样本股票池）
    - 截止日期 `end_date`（YYYY-MM-DD）
    - 运行模式 `mode`：
      - `全部脚本(云端轻量)` → 一次跑 TD九底 + 玄学(云端) + 新指标(云端)
      - `仅 TD 九底(日常)`
      - `仅 玄学(云端)`
      - `仅 新指标(云端)`
  - 日志区域：实时显示 Runner 的标准输出（包括每个脚本的执行过程和索引重建日志）。

---

## 6. Runner 与统一入口的关系

每次你从前端点击“手动运行云端 Runner”时，容器内会做：

1. 按所选模式依次运行：
   - `PlanToDeploy/stream_run_daily.py`
   - `PlanToDeploy/cloud_xuanxue_stream_runner.py`
   - `PlanToDeploy/cloud_new_stream_runner.py`
2. 写入结果到：
   - `PlanToDeploy/output/cloud_td/...`
   - `PlanToDeploy/output/cloud_xuanxue/...`
   - `PlanToDeploy/output/cloud_new/...`
3. 更新索引：
   - `PlanToDeploy/stocks_index/YYYY-MM-DD/stocks_index.csv`
4. **统一入口重建**（关键）：
   - 最后总会执行：`PlanToDeploy/scripts/build_reports_index.py`
   - 输出/覆盖：
     - `PlanToDeploy/report_index/reports_index_YYYY-MM-DD/reports_index.html`
     - `PlanToDeploy/cloud_index/cloud_reports_index.html`
   - `/cloud` 页面就是直接读取 `cloud_index/cloud_reports_index.html`。

> 容器内会自动设置环境变量 `NO_BROWSER=1`，确保这些脚本在服务器上执行时不会尝试弹出本地浏览器窗口。

---

## 7. 自动每日运行（可选）

如果你希望服务器每天定时自动跑云端 Runner，可以在 `docker-compose.yml` 中打开自动运行配置：

```yaml
services:
  stock-cloud:
    ...
    environment:
      API_APP_MODULE: "PlanToDeploy.api_server:app"
      AUTO_RUN_ENABLED: "1"
      AUTO_RUN_HOUR: "3"       # 北京时间凌晨 3 点
      AUTO_RUN_MINUTE: "0"
      # AUTO_RUN_MODE: "all"   # 默认 all，可选: all/daily/xuanxue/new
```

然后重新部署：

```bash
docker compose up -d stock-cloud
```

Flask 进程启动后，会在后台线程中每晚自动调用 Runner，统一入口会持续更新。

---

## 8. 日常运维与更新

### 8.1 查看运行日志

```bash
docker logs --tail=100 stock-cloud
```

如需持续跟踪：

```bash
docker logs -f stock-cloud
```

### 8.2 更新代码与镜像

当你在仓库中修改了代码（例如本地改好再 `git push` 到远程）后，在服务器上：

```bash
cd /srv/stock-fliter
git pull             # 或者同步最新代码
docker compose build
docker compose up -d stock-cloud
```

挂载卷 `./:/app` 确保历史报告与索引不会丢失。

---

## 9. 常见问题

### 9.1 `/cloud` 打开 404

- 确认容器用的是云端版 API：
  - `docker-compose.yml` 中 `API_APP_MODULE` 是否为 `PlanToDeploy.api_server:app`；
- 重新 `docker compose up -d stock-cloud` 后再访问。

### 9.2 报告列表有日期，但某个格子一直是“无信号”

- 说明这一天对应模块确实没有任何命中记录（脚本跑完但没有生成 `summary_*.html` 报告），统一入口会按设计显示“无信号”，这是正常情况。

### 9.3 想在服务器上直接检查生成的文件

所有文件都写在容器挂载的 `/app`（宿主机目录）里，例如：

```bash
cd /srv/stock-fliter

# 云端报告
ls PlanToDeploy/output/cloud_td
ls PlanToDeploy/output/cloud_xuanxue
ls PlanToDeploy/output/cloud_new

# 统一入口 HTML
ls PlanToDeploy/cloud_index
```

你也可以直接用编辑器打开 `PlanToDeploy/cloud_index/cloud_reports_index.html` 查看实际生成的 HTML。

---

