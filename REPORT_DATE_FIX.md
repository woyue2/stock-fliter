# 报告日期显示修复说明

## 📋 问题描述

之前报告列表显示的是**生成日期**（2026-01-31），而不是**数据日期**（2026-01-30）。

### 问题示例
```
错误显示：
日期         稳步上升    TD九底    新指标
2026-01-31   查看        -         查看

正确显示：
日期         稳步上升    TD九底    新指标
2026-01-30   查看        查看      查看
```

## 🔧 修复方案

### 1. 修改 `scripts/build_reports_index.py`

**核心改动**：从文件名中提取数据日期，而不是使用目录名（生成日期）

#### 新增函数
```python
def extract_data_date_from_filename(filename: str) -> str | None:
    """
    从 summary_YYYYMMDD_HHMMSS.html 中提取数据日期 YYYYMMDD，
    转换为 YYYY-MM-DD 格式。
    """
    match = re.match(r"summary_(\d{8})_\d{6}\.html", filename)
    if match:
        date_str = match.group(1)  # YYYYMMDD
        return f"{date_str[:4]}-{date_str[4:6]}-{date_str[6:8]}"
    return None
```

#### 修改 `scan_module()` 函数
- **旧逻辑**：按目录名（生成日期）分组
- **新逻辑**：
  1. 扫描所有 `summary_*.html` 文件
  2. 从文件名提取数据日期
  3. 按数据日期分组
  4. 对每个数据日期，选择最新生成的报告

### 2. 文件输出调整

- **旧行为**：覆盖 `reports_index.html`
- **新行为**：生成 `reports_list.html`（简单列表）
- **原因**：保留 `reports_index.html` 的搜索功能

### 3. 更新 `reports_index.html`

- 保持完整的搜索功能
- 更新内嵌的报告列表数据（使用数据日期）
- 在表头标注"按数据日期排序"

## ✅ 修复结果

### 文件说明

| 文件 | 用途 | 更新方式 |
|------|------|----------|
| `reports_index.html` | 带搜索功能的主页面 | 手动维护（包含搜索UI） |
| `reports_list.html` | 简单报告列表 | 脚本自动生成 |

### 日期显示逻辑

```
目录结构：
check-new-indicators/output/
  └── 2026-01-31/              ← 生成日期（仅作为时间戳）
      └── 01-25-14/
          └── summary_20260130_012514.html  ← 数据日期（用于显示）

显示结果：
数据日期: 2026-01-30  ← 从文件名提取
```

### 报告选择逻辑

当同一数据日期有多次生成时，选择最新的：

```
数据日期 2026-01-30 的报告：
- 生成于 2026-01-30/23-58-57/summary_20260130_235857.html
- 生成于 2026-01-31/01-25-14/summary_20260130_012514.html  ← 选择这个（最新）
```

## 📝 使用说明

### 生成报告列表

```bash
# 运行脚本生成 reports_list.html
python scripts/build_reports_index.py
```

### 更新搜索页面的报告列表

1. 运行脚本生成 `reports_list.html`
2. 打开 `reports_list.html` 复制表格内容
3. 更新 `reports_index.html` 中 `showReportIndex()` 函数的表格数据

或者直接编辑 `reports_index.html` 中的表格数据。

## 🎯 关键原则

**数据日期 vs 生成日期**

- **数据日期**：股票数据的实际日期（如 2026-01-30）
  - 用于：报告列表显示、用户查看
  - 来源：文件名 `summary_20260130_*.html`

- **生成日期**：报告生成的时间戳（如 2026-01-31）
  - 用于：目录组织、版本管理
  - 来源：目录名 `2026-01-31/01-25-14/`

**显示原则**：始终以数据日期为准，生成日期仅作为内部时间戳。

## 🔍 验证方法

1. 打开 `reports_index.html`
2. 点击"报告列表"按钮
3. 检查日期列是否显示数据日期（2026-01-30）而不是生成日期（2026-01-31）

## 📅 修复日期

2026-01-31

## 👤 修复原因

用户反馈：不应该存在 2026-01-31 的数据，因为手里的数据最新是 1月30号。所有显示都应该以数据日期为准，而不是生成日期。生成日期只是作为时间戳给本地看谁最先生成。

